# Athlete Tracker — Cliente

SPA React + Vite. Documentación completa, setup y deploy en el [**README de la raíz**](../README.md).

```bash
cp .env.example .env
npm install
npm run dev
```

Demo: `demo@athlete-tracker.dev` / `Demo1234` (requiere API + seed).
